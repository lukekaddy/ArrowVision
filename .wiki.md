# Project Summary
**ArrowLive** is a mobile-first web application tailored for managing and scoring archery tournaments, transitioning from traditional paper scorecards to a comprehensive digital platform. It offers features such as live scoring, real-time leaderboards, and instant results, enhancing the experience for tournament organizers, archers, and scorekeepers. The platform now supports advanced features like mulligan options, allowing for flexible scoring rules, catering to diverse tournament formats.

# Project Module Description
- **Authentication**: User authentication via Atoms Cloud.
- **Tournament Management**: Tools for organizers to create, edit, and manage tournaments, including naming courses and specifying target counts, with new options for mulligans.
- **Live Scoring**: Real-time input and updates of scores.
- **Leaderboard**: Dynamic leaderboard showcasing current rankings, with course filtering capabilities.
- **Smart Score**: AI-assisted scoring feature utilizing image capture technology.
- **Results Export**: Functionality to export tournament results in JSON format.
- **Archer Registration**: Enhanced registration form including first name, last name, phone number, and division selection.

# Directory Tree
```
app/
├── backend/
│   ├── models/                  # SQLAlchemy ORM models (tournaments, tournament_archers, scores)
│   ├── services/                # Business logic (including tournament_ops.py)
│   ├── routers/                 # API endpoints (including tournament_ops.py)
│   ├── data_models/             # JSON data models for scores and tournaments
│   └── core/                    # Core functionalities (authentication, database)
└── frontend/
    ├── src/
    │   ├── lib/                 # Client utilities for API calls
    │   ├── contexts/            # React contexts for global state management
    │   ├── components/          # Reusable UI components
    │   └── pages/               # Main application pages (Index, TournamentCreate, TournamentDashboard, Leaderboard, Scorecard)
```

# File Description Inventory
- **backend/**: Contains the backend logic, including models, services, and API routers.
- **frontend/**: Contains the frontend application built with React, including UI components and pages.
- **README.md**: Documentation for both frontend and backend modules.
- **requirements.txt**: Lists dependencies for both frontend and backend.

# Technology Stack
- **Frontend**: React, TypeScript, Vite, Tailwind CSS, shadcn/ui
- **Backend**: Atoms Cloud (FastAPI, PostgreSQL, Authentication)
- **SDK**: @metagptx/web-sdk for authentication and API calls

# Usage
1. **Install Dependencies**:
   - For the backend: Run `pip install -r requirements.txt`
   - For the frontend: Run `pnpm install`
   
2. **Build the Project**:
   - For the backend, ensure the database is set up and migrations are applied.
   - For the frontend, run `pnpm run build` to compile the application.

3. **Run the Application**:
   - Start the backend server.
   - Start the frontend server to access the web application.
