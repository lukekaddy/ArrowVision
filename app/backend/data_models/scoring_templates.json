{
    "title": "scoring_templates",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "User ID of creator"
        },
        "tournament_id": {
            "type": "integer",
            "description": "Tournament this template belongs to"
        },
        "template_name": {
            "type": "string",
            "description": "Name of the scoring template"
        },
        "score_values": {
            "type": "string",
            "description": "JSON array of score values e.g. [10,8,5,0]"
        },
        "is_custom": {
            "type": "boolean",
            "description": "Whether this is a custom template"
        }
    },
    "required": [
        "id",
        "user_id",
        "tournament_id",
        "template_name",
        "score_values"
    ]
}